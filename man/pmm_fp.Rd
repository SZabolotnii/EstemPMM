% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmm_fp.R
\name{pmm_fp}
\alias{pmm_fp}
\title{Polynomial Maximization Method with Fractional Polynomial basis}
\usage{
pmm_fp(
  formula,
  data,
  track = c("pos", "full"),
  order = 2,
  max_terms = 4,
  criterion = c("BIC", "AIC"),
  shift_eps = NULL
)
}
\arguments{
\item{formula}{a two-sided formula of the form \code{y ~ x} with exactly one
numeric predictor on the right-hand side. The intercept is added
automatically through the FP design.}

\item{data}{a data.frame containing \code{y} and \code{x}.}

\item{track}{character: \code{"pos"} (default) or \code{"full"}. Selects the basis.}

\item{order}{integer PMM order; currently only \code{order = 2} is supported,
\code{order = 3} is reserved for the symmetric platykurtic case.}

\item{max_terms}{integer maximum number of FP terms in a candidate model
(default 4; see frozen decision D2).}

\item{criterion}{character: \code{"BIC"} (default) or \code{"AIC"}.}

\item{shift_eps}{numeric. If \code{min(x) <= 0}, \code{x} is shifted by
\code{-min(x) + shift_eps}. When \code{NULL}, a default \code{1e-3} is used.}
}
\value{
an S4 object of class \code{PMM_FP_fit} inheriting from \code{PMM2fit}.
}
\description{
Fits a fractional-polynomial regression model with PMM2 (or PMM3, future)
estimation of the coefficients. The procedure enumerates subsets of an
FP basis (Royston-Altman 1994), refits each candidate with \code{\link[=lm_pmm2]{lm_pmm2()}},
and selects the minimiser of the chosen information criterion.
}
\details{
Two basis tracks are supported (see Zabolotnii 2026, PMM-FP article,
frozen decision D1v2):
\itemize{
\item \code{track = "pos"} — \verb{P_a = \{0, 0.5, 1, 2, 3\}} (K = 10 with companions),
requires \code{E[xi^4] < inf}.
\item \code{track = "full"} — \verb{P_b = \{-2, -1, -0.5, 0, 0.5, 1, 2, 3\}} (K = 16),
additionally requires bounded density at zero and \verb{E[|xi|^-2] < inf}.
}

The current implementation is a scaffold: it generates candidate models
and the design matrix, but only the first candidate is refit with PMM2.
Full PMM2 sweep and information-criterion comparison are planned for the
next iteration (see TODO markers in source).
}
\examples{
\dontrun{
  set.seed(20260511)
  n <- 50
  x <- runif(n, 0.5, 5)
  y <- 1 + 2 * x ^ 0.5 + (rgamma(n, 2) - 2)
  fit <- pmm_fp(y ~ x, data = data.frame(x = x, y = y),
                track = "pos", max_terms = 2)
  fit@selected_powers
}

}
\references{
Royston P, Altman DG (1994). Regression using fractional polynomials of
continuous covariates: parsimonious parametric modelling. Appl. Statist.
43(3), 429-467.

Hubin A, Krogdahl T, Lokka A, Kortner T (2026). Bayesian Fractional
Polynomials for Optimal Dosage Estimation. arXiv:2605.06237.

Zabolotnii S (2026). Polynomial Maximization Method with Fractional
Polynomial Basis. In preparation.
}
